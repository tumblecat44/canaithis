name: Production smoke

on:
  push:
    branches: [main]
  schedule:
    - cron: "*/30 * * * *"
  workflow_dispatch:

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Check homepage
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" https://canaithis.vercel.app/ko)
          echo "/ko → $code"
          test "$code" = "200"

      - name: Check homepage RSS alternate link
        run: |
          home_html=$(curl -sL https://canaithis.vercel.app/ko)
          rss_link=$(echo "$home_html" | grep -oE '<link[^>]*application/rss\+xml[^>]*>' || true)
          test -n "$rss_link"
          echo "$rss_link" | grep -q 'rel="alternate"'
          echo "$rss_link" | grep -qE 'href="[^"]*/feed\.xml"'
          echo "/ko head → RSS alternate link OK"

      - name: Check English homepage RSS alternate link
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" https://canaithis.vercel.app/en)
          echo "/en → $code"
          test "$code" = "200"
          en_html=$(curl -sL https://canaithis.vercel.app/en)
          rss_link=$(echo "$en_html" | grep -oE '<link[^>]*application/rss\+xml[^>]*>' || true)
          test -n "$rss_link"
          echo "$rss_link" | grep -q 'rel="alternate"'
          echo "$rss_link" | grep -qE 'href="[^"]*/feed\.xml"'
          echo "/en head → RSS alternate link OK"

      - name: Check homepage challenge cards (DB)
        run: |
          home_html=$(curl -sL https://canaithis.vercel.app/ko)
          challenge_id=$(
            echo "$home_html" \
              | grep -oE 'challenges/[a-f0-9]{24}' \
              | grep -v '/new' \
              | head -1 \
              | sed 's|challenges/||'
          )
          test -n "$challenge_id"
          echo "/ko challenge card → challenges/${challenge_id}"
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/ko/challenges/${challenge_id}")
          echo "/ko/challenges/${challenge_id} → $code"
          test "$code" = "200"

      - name: Check English homepage challenge cards (DB)
        run: |
          en_html=$(curl -sL https://canaithis.vercel.app/en)
          challenge_id=$(
            echo "$en_html" \
              | grep -oE 'challenges/[a-f0-9]{24}' \
              | grep -v '/new' \
              | head -1 \
              | sed 's|challenges/||'
          )
          test -n "$challenge_id"
          echo "/en challenge card → challenges/${challenge_id}"
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/en/challenges/${challenge_id}")
          echo "/en/challenges/${challenge_id} → $code"
          test "$code" = "200"

      - name: Check login page
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" https://canaithis.vercel.app/ko/login)
          echo "/ko/login → $code"
          test "$code" = "200"

      - name: Check English login page
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" https://canaithis.vercel.app/en/login)
          echo "/en/login → $code"
          test "$code" = "200"

      - name: Check profile redirects to login
        run: |
          hdr=$(mktemp)
          code=$(curl -s -o /dev/null -D "$hdr" -w "%{http_code}" "https://canaithis.vercel.app/ko/profile")
          location=$(grep -i "^location:" "$hdr" | tr -d '\r' || true)
          rm -f "$hdr"
          echo "/ko/profile → $code $location"
          test "$code" = "307"
          echo "$location" | grep -qi "/login"
          echo "$location" | grep -qi "callbackUrl"

      - name: Check English profile redirects to login
        run: |
          hdr=$(mktemp)
          code=$(curl -s -o /dev/null -D "$hdr" -w "%{http_code}" "https://canaithis.vercel.app/en/profile")
          location=$(grep -i "^location:" "$hdr" | tr -d '\r' || true)
          rm -f "$hdr"
          echo "/en/profile → $code $location"
          test "$code" = "307"
          echo "$location" | grep -qi "/login"
          echo "$location" | grep -qi "callbackUrl"

      - name: Check challenges new redirects to login
        run: |
          hdr=$(mktemp)
          code=$(curl -s -o /dev/null -D "$hdr" -w "%{http_code}" "https://canaithis.vercel.app/ko/challenges/new")
          location=$(grep -i "^location:" "$hdr" | tr -d '\r' || true)
          rm -f "$hdr"
          echo "/ko/challenges/new → $code $location"
          test "$code" = "307"
          echo "$location" | grep -qi "/login"
          echo "$location" | grep -qi "callbackUrl"

      - name: Check English challenges new redirects to login
        run: |
          hdr=$(mktemp)
          code=$(curl -s -o /dev/null -D "$hdr" -w "%{http_code}" "https://canaithis.vercel.app/en/challenges/new")
          location=$(grep -i "^location:" "$hdr" | tr -d '\r' || true)
          rm -f "$hdr"
          echo "/en/challenges/new → $code $location"
          test "$code" = "307"
          echo "$location" | grep -qi "/login"
          echo "$location" | grep -qi "callbackUrl"

      - name: Check RSS feed
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" https://canaithis.vercel.app/feed.xml)
          echo "/feed.xml → $code"
          test "$code" = "200"
          feed_body=$(curl -sL https://canaithis.vercel.app/feed.xml)
          echo "$feed_body" | head -1 | grep -q "<?xml"
          channel_block=$(echo "$feed_body" | sed -n '/<channel>/,/<item>/p' | head -10)
          echo "$channel_block" | grep -qE '<title>CanAIThis</title>'
          echo "feed.xml channel → title OK"
          echo "$channel_block" | grep -qE '<link>https?://[^<]+</link>'
          echo "feed.xml channel → link OK"
          echo "$channel_block" | grep -qE '<description>AI challenges and solutions community</description>'
          echo "feed.xml channel → description OK"
          echo "$channel_block" | grep -qE '<language>ko</language>'
          echo "feed.xml channel → language OK"
          item_count=$(echo "$feed_body" | grep -c '<item>' || true)
          test "$item_count" -ge 1
          echo "feed.xml → ${item_count} RSS item(s) OK"
          first_item=$(echo "$feed_body" | sed -n '/<item>/,/<\/item>/p' | head -20)
          echo "$first_item" | grep -qE '<title>[^<]{2,}</title>'
          echo "feed.xml item → title OK"
          echo "$first_item" | grep -qE '<link>https?://[^<]+/challenges/[a-f0-9]{24}</link>'
          echo "feed.xml item → link OK"
          echo "$first_item" | grep -qE '<guid[^>]*>https?://[^<]+/challenges/[a-f0-9]{24}</guid>'
          echo "feed.xml item → guid OK"
          echo "$first_item" | grep -qE '<pubDate>[A-Za-z]{3}, [0-9]{2} [A-Za-z]{3} [0-9]{4} [0-9]{2}:[0-9]{2}:[0-9]{2} GMT</pubDate>'
          echo "feed.xml item → pubDate OK"
          echo "$first_item" | grep -qE '<description>[^<]{2,}</description>'
          echo "feed.xml item → description OK"

      - name: Check challenge edit redirects to login
        run: |
          challenge_id=$(
            curl -sL https://canaithis.vercel.app/feed.xml \
              | grep -oE 'challenges/[a-f0-9]{24}' \
              | head -1 \
              | sed 's|challenges/||'
          )
          test -n "$challenge_id"
          echo "challenge id from feed.xml → $challenge_id"
          hdr=$(mktemp)
          code=$(curl -s -o /dev/null -D "$hdr" -w "%{http_code}" "https://canaithis.vercel.app/ko/challenges/${challenge_id}/edit")
          location=$(grep -i "^location:" "$hdr" | tr -d '\r' || true)
          rm -f "$hdr"
          echo "/ko/challenges/${challenge_id}/edit → $code $location"
          test "$code" = "307"
          echo "$location" | grep -qi "/login"
          echo "$location" | grep -qi "callbackUrl"

      - name: Check English challenge edit redirects to login
        run: |
          en_html=$(curl -sL https://canaithis.vercel.app/en)
          challenge_id=$(
            echo "$en_html" \
              | grep -oE 'challenges/[a-f0-9]{24}' \
              | grep -v '/new' \
              | head -1 \
              | sed 's|challenges/||'
          )
          test -n "$challenge_id"
          echo "challenge id from /en → $challenge_id"
          hdr=$(mktemp)
          code=$(curl -s -o /dev/null -D "$hdr" -w "%{http_code}" "https://canaithis.vercel.app/en/challenges/${challenge_id}/edit")
          location=$(grep -i "^location:" "$hdr" | tr -d '\r' || true)
          rm -f "$hdr"
          echo "/en/challenges/${challenge_id}/edit → $code $location"
          test "$code" = "307"
          echo "$location" | grep -qi "/login"
          echo "$location" | grep -qi "callbackUrl"

      - name: Check solution new redirects to login
        run: |
          challenge_id=$(
            curl -sL https://canaithis.vercel.app/feed.xml \
              | grep -oE 'challenges/[a-f0-9]{24}' \
              | head -1 \
              | sed 's|challenges/||'
          )
          test -n "$challenge_id"
          echo "challenge id from feed.xml → $challenge_id"
          hdr=$(mktemp)
          code=$(curl -s -o /dev/null -D "$hdr" -w "%{http_code}" "https://canaithis.vercel.app/ko/challenges/${challenge_id}/solutions/new")
          location=$(grep -i "^location:" "$hdr" | tr -d '\r' || true)
          rm -f "$hdr"
          echo "/ko/challenges/${challenge_id}/solutions/new → $code $location"
          test "$code" = "307"
          echo "$location" | grep -qi "/login"
          echo "$location" | grep -qi "callbackUrl"

      - name: Check English solution new redirects to login
        run: |
          challenge_id=$(
            curl -sL https://canaithis.vercel.app/feed.xml \
              | grep -oE 'challenges/[a-f0-9]{24}' \
              | head -1 \
              | sed 's|challenges/||'
          )
          test -n "$challenge_id"
          echo "challenge id from feed.xml → $challenge_id"
          hdr=$(mktemp)
          code=$(curl -s -o /dev/null -D "$hdr" -w "%{http_code}" "https://canaithis.vercel.app/en/challenges/${challenge_id}/solutions/new")
          location=$(grep -i "^location:" "$hdr" | tr -d '\r' || true)
          rm -f "$hdr"
          echo "/en/challenges/${challenge_id}/solutions/new → $code $location"
          test "$code" = "307"
          echo "$location" | grep -qi "/login"
          echo "$location" | grep -qi "callbackUrl"

      - name: Check solution edit redirects to login
        run: |
          home_html=$(curl -sL https://canaithis.vercel.app/ko)
          solution_id=$(
            echo "$home_html" \
              | grep -oE 'ACTION_ID_[a-f0-9]{24}' \
              | head -1 \
              | sed 's|ACTION_ID_||'
          )
          test -n "$solution_id"
          echo "solution id from /ko → $solution_id"
          edit_found=0
          while read -r challenge_id; do
            [ -n "$challenge_id" ] || continue
            hdr=$(mktemp)
            code=$(curl -s -o /dev/null -D "$hdr" -w "%{http_code}" "https://canaithis.vercel.app/ko/challenges/${challenge_id}/solutions/${solution_id}/edit")
            location=$(grep -i "^location:" "$hdr" | tr -d '\r' || true)
            rm -f "$hdr"
            if [ "$code" = "307" ] && echo "$location" | grep -qi "/login"; then
              echo "/ko/challenges/${challenge_id}/solutions/${solution_id}/edit → $code $location"
              echo "$location" | grep -qi "callbackUrl"
              edit_found=1
              break
            fi
          done < <(
            echo "$home_html" \
              | grep -oE 'challenges/[a-f0-9]{24}' \
              | grep -v '/new' \
              | sed 's|challenges/||' \
              | awk '!seen[$0]++'
          )
          test "$edit_found" = "1"

      - name: Check English solution edit redirects to login
        run: |
          en_html=$(curl -sL https://canaithis.vercel.app/en)
          solution_id=$(
            echo "$en_html" \
              | grep -oE 'ACTION_ID_[a-f0-9]{24}' \
              | head -1 \
              | sed 's|ACTION_ID_||'
          )
          test -n "$solution_id"
          echo "solution id from /en → $solution_id"
          edit_found=0
          while read -r challenge_id; do
            [ -n "$challenge_id" ] || continue
            hdr=$(mktemp)
            code=$(curl -s -o /dev/null -D "$hdr" -w "%{http_code}" "https://canaithis.vercel.app/en/challenges/${challenge_id}/solutions/${solution_id}/edit")
            location=$(grep -i "^location:" "$hdr" | tr -d '\r' || true)
            rm -f "$hdr"
            if [ "$code" = "307" ] && echo "$location" | grep -qi "/login"; then
              echo "/en/challenges/${challenge_id}/solutions/${solution_id}/edit → $code $location"
              echo "$location" | grep -qi "callbackUrl"
              edit_found=1
              break
            fi
          done < <(
            echo "$en_html" \
              | grep -oE 'challenges/[a-f0-9]{24}' \
              | grep -v '/new' \
              | sed 's|challenges/||' \
              | awk '!seen[$0]++'
          )
          test "$edit_found" = "1"

      - name: Check invalid user 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" https://canaithis.vercel.app/ko/users/not-a-valid-user)
          echo "/ko/users/invalid → $code"
          test "$code" = "404"

      - name: Check English invalid user 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" https://canaithis.vercel.app/en/users/not-a-valid-user)
          echo "/en/users/invalid → $code"
          test "$code" = "404"

      - name: Check invalid locale 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" https://canaithis.vercel.app/xx)
          echo "/xx (invalid locale) → $code"
          test "$code" = "404"

      - name: Check invalid locale home sort views 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx?sort=views")
          echo "/xx?sort=views (invalid locale home + query) → $code"
          test "$code" = "404"

      - name: Check invalid locale home sort popular 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx?sort=popular")
          echo "/xx?sort=popular (invalid locale home + query) → $code"
          test "$code" = "404"

      - name: Check invalid locale home sort latest 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx?sort=latest")
          echo "/xx?sort=latest (invalid locale home + query) → $code"
          test "$code" = "404"

      - name: Check invalid locale home search 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx?q=test")
          echo "/xx?q=test (invalid locale home + search) → $code"
          test "$code" = "404"

      - name: Check invalid locale home search category 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx?q=test&category=design")
          echo "/xx?q=test&category=design (invalid locale home + search + category) → $code"
          test "$code" = "404"

      - name: Check invalid locale home search category sort latest 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx?q=test&category=design&sort=latest")
          echo "/xx?q=test&category=design&sort=latest (invalid locale home + search + category + sort) → $code"
          test "$code" = "404"

      - name: Check invalid locale home search category sort popular 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx?q=test&category=design&sort=popular")
          echo "/xx?q=test&category=design&sort=popular (invalid locale home + search + category + sort) → $code"
          test "$code" = "404"

      - name: Check invalid locale home search category sort views 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx?q=test&category=design&sort=views")
          echo "/xx?q=test&category=design&sort=views (invalid locale home + search + category + sort) → $code"
          test "$code" = "404"

      - name: Check invalid locale home search sort latest 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx?q=test&sort=latest")
          echo "/xx?q=test&sort=latest (invalid locale home + search + sort) → $code"
          test "$code" = "404"

      - name: Check invalid locale home search sort popular 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx?q=test&sort=popular")
          echo "/xx?q=test&sort=popular (invalid locale home + search + sort) → $code"
          test "$code" = "404"

      - name: Check invalid locale home search sort views 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx?q=test&sort=views")
          echo "/xx?q=test&sort=views (invalid locale home + search + sort) → $code"
          test "$code" = "404"

      - name: Check invalid locale home category 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx?category=design")
          echo "/xx?category=design (invalid locale home + category) → $code"
          test "$code" = "404"

      - name: Check invalid locale home category coding 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx?category=coding")
          echo "/xx?category=coding (invalid locale home + coding category) → $code"
          test "$code" = "404"

      - name: Check invalid locale home category coding sort latest 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx?category=coding&sort=latest")
          echo "/xx?category=coding&sort=latest (invalid locale home + coding category + latest sort) → $code"
          test "$code" = "404"

      - name: Check invalid locale home category coding sort popular 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx?category=coding&sort=popular")
          echo "/xx?category=coding&sort=popular (invalid locale home + coding category + popular sort) → $code"
          test "$code" = "404"

      - name: Check invalid locale home category coding sort views 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx?category=coding&sort=views")
          echo "/xx?category=coding&sort=views (invalid locale home + coding category + views sort) → $code"
          test "$code" = "404"

      - name: Check invalid locale home category sort latest 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx?category=design&sort=latest")
          echo "/xx?category=design&sort=latest (invalid locale home + category + sort) → $code"
          test "$code" = "404"

      - name: Check invalid locale home category sort popular 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx?category=design&sort=popular")
          echo "/xx?category=design&sort=popular (invalid locale home + category + popular sort) → $code"
          test "$code" = "404"

      - name: Check invalid locale home category sort views 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx?category=design&sort=views")
          echo "/xx?category=design&sort=views (invalid locale home + category + views sort) → $code"
          test "$code" = "404"

      - name: Check invalid locale home page 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx?page=2")
          echo "/xx?page=2 (invalid locale home + pagination) → $code"
          test "$code" = "404"

      - name: Check invalid locale home page sort latest 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx?page=2&sort=latest")
          echo "/xx?page=2&sort=latest (invalid locale home + pagination + latest sort) → $code"
          test "$code" = "404"

      - name: Check invalid locale home page sort popular 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx?page=2&sort=popular")
          echo "/xx?page=2&sort=popular (invalid locale home + pagination + popular sort) → $code"
          test "$code" = "404"

      - name: Check invalid locale home page sort views 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx?page=2&sort=views")
          echo "/xx?page=2&sort=views (invalid locale home + pagination + views sort) → $code"
          test "$code" = "404"

      - name: Check invalid locale home page search 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx?page=2&q=test")
          echo "/xx?page=2&q=test (invalid locale home + pagination + search) → $code"
          test "$code" = "404"

      - name: Check invalid locale home page search sort latest 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx?page=2&q=test&sort=latest")
          echo "/xx?page=2&q=test&sort=latest (invalid locale home + pagination + search + latest sort) → $code"
          test "$code" = "404"

      - name: Check invalid locale home page search sort popular 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx?page=2&q=test&sort=popular")
          echo "/xx?page=2&q=test&sort=popular (invalid locale home + pagination + search + popular sort) → $code"
          test "$code" = "404"

      - name: Check invalid locale home page search sort views 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx?page=2&q=test&sort=views")
          echo "/xx?page=2&q=test&sort=views (invalid locale home + pagination + search + views sort) → $code"
          test "$code" = "404"

      - name: Check invalid locale home page search category 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx?page=2&q=test&category=design")
          echo "/xx?page=2&q=test&category=design (invalid locale home + pagination + search + category) → $code"
          test "$code" = "404"

      - name: Check invalid locale home page search category sort latest 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx?page=2&q=test&category=design&sort=latest")
          echo "/xx?page=2&q=test&category=design&sort=latest (invalid locale home + pagination + search + category + latest sort) → $code"
          test "$code" = "404"

      - name: Check invalid locale home page search category sort popular 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx?page=2&q=test&category=design&sort=popular")
          echo "/xx?page=2&q=test&category=design&sort=popular (invalid locale home + pagination + search + category + popular sort) → $code"
          test "$code" = "404"

      - name: Check invalid locale home page search category sort views 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx?page=2&q=test&category=design&sort=views")
          echo "/xx?page=2&q=test&category=design&sort=views (invalid locale home + pagination + search + category + views sort) → $code"
          test "$code" = "404"

      - name: Check invalid locale home page category 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx?page=2&category=design")
          echo "/xx?page=2&category=design (invalid locale home + pagination + category) → $code"
          test "$code" = "404"

      - name: Check invalid locale home page category sort latest 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx?page=2&category=design&sort=latest")
          echo "/xx?page=2&category=design&sort=latest (invalid locale home + pagination + category + sort) → $code"
          test "$code" = "404"

      - name: Check invalid locale home page category sort popular 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx?page=2&category=design&sort=popular")
          echo "/xx?page=2&category=design&sort=popular (invalid locale home + pagination + category + popular sort) → $code"
          test "$code" = "404"

      - name: Check invalid locale home page category sort views 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx?page=2&category=design&sort=views")
          echo "/xx?page=2&category=design&sort=views (invalid locale home + pagination + category + views sort) → $code"
          test "$code" = "404"

      - name: Check invalid locale nested challenges category 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx/challenges?category=design")
          echo "/xx/challenges?category=design (invalid locale nested + category) → $code"
          test "$code" = "404"

      - name: Check invalid locale nested challenges category sort latest 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx/challenges?category=design&sort=latest")
          echo "/xx/challenges?category=design&sort=latest (invalid locale nested + category + sort) → $code"
          test "$code" = "404"

      - name: Check invalid locale nested challenges category sort popular 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx/challenges?category=design&sort=popular")
          echo "/xx/challenges?category=design&sort=popular (invalid locale nested + category + popular sort) → $code"
          test "$code" = "404"

      - name: Check invalid locale nested challenges category sort views 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx/challenges?category=design&sort=views")
          echo "/xx/challenges?category=design&sort=views (invalid locale nested + category + views sort) → $code"
          test "$code" = "404"

      - name: Check invalid locale nested challenges category coding 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx/challenges?category=coding")
          echo "/xx/challenges?category=coding (invalid locale nested + coding category) → $code"
          test "$code" = "404"

      - name: Check invalid locale nested challenges category coding sort latest 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx/challenges?category=coding&sort=latest")
          echo "/xx/challenges?category=coding&sort=latest (invalid locale nested + coding category + latest sort) → $code"
          test "$code" = "404"

      - name: Check invalid locale nested challenges category coding sort popular 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx/challenges?category=coding&sort=popular")
          echo "/xx/challenges?category=coding&sort=popular (invalid locale nested + coding category + popular sort) → $code"
          test "$code" = "404"

      - name: Check invalid locale nested challenges 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" https://canaithis.vercel.app/xx/challenges)
          echo "/xx/challenges (invalid locale nested) → $code"
          test "$code" = "404"

      - name: Check invalid locale nested challenges page 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx/challenges?page=2")
          echo "/xx/challenges?page=2 (invalid locale nested + pagination) → $code"
          test "$code" = "404"

      - name: Check invalid locale nested challenges page sort latest 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx/challenges?page=2&sort=latest")
          echo "/xx/challenges?page=2&sort=latest (invalid locale nested + pagination + latest sort) → $code"
          test "$code" = "404"

      - name: Check invalid locale nested challenges page sort popular 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx/challenges?page=2&sort=popular")
          echo "/xx/challenges?page=2&sort=popular (invalid locale nested + pagination + popular sort) → $code"
          test "$code" = "404"

      - name: Check invalid locale nested challenges page sort views 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx/challenges?page=2&sort=views")
          echo "/xx/challenges?page=2&sort=views (invalid locale nested + pagination + views sort) → $code"
          test "$code" = "404"

      - name: Check invalid locale nested challenges page search 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx/challenges?page=2&q=test")
          echo "/xx/challenges?page=2&q=test (invalid locale nested + pagination + search) → $code"
          test "$code" = "404"

      - name: Check invalid locale nested challenges page search sort latest 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx/challenges?page=2&q=test&sort=latest")
          echo "/xx/challenges?page=2&q=test&sort=latest (invalid locale nested + pagination + search + latest sort) → $code"
          test "$code" = "404"

      - name: Check invalid locale nested challenges page search sort popular 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx/challenges?page=2&q=test&sort=popular")
          echo "/xx/challenges?page=2&q=test&sort=popular (invalid locale nested + pagination + search + popular sort) → $code"
          test "$code" = "404"

      - name: Check invalid locale nested challenges page search sort views 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx/challenges?page=2&q=test&sort=views")
          echo "/xx/challenges?page=2&q=test&sort=views (invalid locale nested + pagination + search + views sort) → $code"
          test "$code" = "404"

      - name: Check invalid locale nested challenges page search category 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx/challenges?page=2&q=test&category=design")
          echo "/xx/challenges?page=2&q=test&category=design (invalid locale nested + pagination + search + category) → $code"
          test "$code" = "404"

      - name: Check invalid locale nested challenges page search category sort latest 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx/challenges?page=2&q=test&category=design&sort=latest")
          echo "/xx/challenges?page=2&q=test&category=design&sort=latest (invalid locale nested + pagination + search + category + latest sort) → $code"
          test "$code" = "404"

      - name: Check invalid locale nested challenges page search category sort popular 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx/challenges?page=2&q=test&category=design&sort=popular")
          echo "/xx/challenges?page=2&q=test&category=design&sort=popular (invalid locale nested + pagination + search + category + popular sort) → $code"
          test "$code" = "404"

      - name: Check invalid locale nested challenges page search category sort views 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx/challenges?page=2&q=test&category=design&sort=views")
          echo "/xx/challenges?page=2&q=test&category=design&sort=views (invalid locale nested + pagination + search + category + views sort) → $code"
          test "$code" = "404"

      - name: Check invalid locale nested challenges page category 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx/challenges?page=2&category=design")
          echo "/xx/challenges?page=2&category=design (invalid locale nested + pagination + category) → $code"
          test "$code" = "404"

      - name: Check invalid locale nested challenges page category sort latest 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx/challenges?page=2&category=design&sort=latest")
          echo "/xx/challenges?page=2&category=design&sort=latest (invalid locale nested + pagination + category + latest sort) → $code"
          test "$code" = "404"

      - name: Check invalid locale nested challenges page category sort popular 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx/challenges?page=2&category=design&sort=popular")
          echo "/xx/challenges?page=2&category=design&sort=popular (invalid locale nested + pagination + category + popular sort) → $code"
          test "$code" = "404"

      - name: Check invalid locale nested challenges page category sort views 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx/challenges?page=2&category=design&sort=views")
          echo "/xx/challenges?page=2&category=design&sort=views (invalid locale nested + pagination + category + views sort) → $code"
          test "$code" = "404"

      - name: Check invalid locale nested login 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" https://canaithis.vercel.app/xx/login)
          echo "/xx/login (invalid locale nested) → $code"
          test "$code" = "404"

      - name: Check invalid locale nested profile 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" https://canaithis.vercel.app/xx/profile)
          echo "/xx/profile (invalid locale nested) → $code"
          test "$code" = "404"

      - name: Check invalid locale nested challenges new 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" https://canaithis.vercel.app/xx/challenges/new)
          echo "/xx/challenges/new (invalid locale nested) → $code"
          test "$code" = "404"

      - name: Check invalid locale nested challenge detail 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" https://canaithis.vercel.app/xx/challenges/invalid)
          echo "/xx/challenges/invalid (invalid locale nested) → $code"
          test "$code" = "404"

      - name: Check invalid locale nested challenge edit 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" https://canaithis.vercel.app/xx/challenges/invalid/edit)
          echo "/xx/challenges/invalid/edit (invalid locale nested) → $code"
          test "$code" = "404"

      - name: Check invalid locale nested solution new 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" https://canaithis.vercel.app/xx/challenges/invalid/solutions/new)
          echo "/xx/challenges/invalid/solutions/new (invalid locale nested) → $code"
          test "$code" = "404"

      - name: Check invalid locale nested solution edit 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" https://canaithis.vercel.app/xx/challenges/invalid/solutions/invalid/edit)
          echo "/xx/challenges/invalid/solutions/invalid/edit (invalid locale nested) → $code"
          test "$code" = "404"

      - name: Check invalid locale nested user 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" https://canaithis.vercel.app/xx/users/not-a-valid-user)
          echo "/xx/users/not-a-valid-user (invalid locale nested) → $code"
          test "$code" = "404"

      - name: Check invalid locale nested feed.xml 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" https://canaithis.vercel.app/xx/feed.xml)
          echo "/xx/feed.xml (invalid locale nested) → $code"
          test "$code" = "404"

      - name: Check invalid locale nested sitemap.xml 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" https://canaithis.vercel.app/xx/sitemap.xml)
          echo "/xx/sitemap.xml (invalid locale nested) → $code"
          test "$code" = "404"

      - name: Check invalid locale nested robots.txt 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" https://canaithis.vercel.app/xx/robots.txt)
          echo "/xx/robots.txt (invalid locale nested) → $code"
          test "$code" = "404"

      - name: Check invalid locale nested challenges sort views 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx/challenges?sort=views")
          echo "/xx/challenges?sort=views (invalid locale nested + query) → $code"
          test "$code" = "404"

      - name: Check invalid locale nested challenges sort popular 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx/challenges?sort=popular")
          echo "/xx/challenges?sort=popular (invalid locale nested + query) → $code"
          test "$code" = "404"

      - name: Check invalid locale nested challenges sort latest 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx/challenges?sort=latest")
          echo "/xx/challenges?sort=latest (invalid locale nested + query) → $code"
          test "$code" = "404"

      - name: Check invalid locale nested challenges search 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx/challenges?q=test")
          echo "/xx/challenges?q=test (invalid locale nested + search) → $code"
          test "$code" = "404"

      - name: Check invalid locale nested challenges search sort latest 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx/challenges?q=test&sort=latest")
          echo "/xx/challenges?q=test&sort=latest (invalid locale nested + search + sort) → $code"
          test "$code" = "404"

      - name: Check invalid locale nested challenges search sort popular 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx/challenges?q=test&sort=popular")
          echo "/xx/challenges?q=test&sort=popular (invalid locale nested + search + sort) → $code"
          test "$code" = "404"

      - name: Check invalid locale nested challenges search sort views 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx/challenges?q=test&sort=views")
          echo "/xx/challenges?q=test&sort=views (invalid locale nested + search + sort) → $code"
          test "$code" = "404"

      - name: Check invalid locale nested challenges search category 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx/challenges?q=test&category=design")
          echo "/xx/challenges?q=test&category=design (invalid locale nested + search + category) → $code"
          test "$code" = "404"

      - name: Check invalid locale nested challenges search category sort latest 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx/challenges?q=test&category=design&sort=latest")
          echo "/xx/challenges?q=test&category=design&sort=latest (invalid locale nested + search + category + sort) → $code"
          test "$code" = "404"

      - name: Check invalid locale nested challenges search category sort popular 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx/challenges?q=test&category=design&sort=popular")
          echo "/xx/challenges?q=test&category=design&sort=popular (invalid locale nested + search + category + sort) → $code"
          test "$code" = "404"

      - name: Check invalid locale nested challenges search category sort views 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" "https://canaithis.vercel.app/xx/challenges?q=test&category=design&sort=views")
          echo "/xx/challenges?q=test&category=design&sort=views (invalid locale nested + search + category + sort) → $code"
          test "$code" = "404"

      - name: Check invalid challenge 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" https://canaithis.vercel.app/ko/challenges/invalid)
          echo "/ko/challenges/invalid → $code"
          test "$code" = "404"

      - name: Check English invalid challenge 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" https://canaithis.vercel.app/en/challenges/invalid)
          echo "/en/challenges/invalid → $code"
          test "$code" = "404"

      - name: Check invalid challenge edit 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" https://canaithis.vercel.app/ko/challenges/invalid/edit)
          echo "/ko/challenges/invalid/edit → $code"
          test "$code" = "404"

      - name: Check English invalid challenge edit 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" https://canaithis.vercel.app/en/challenges/invalid/edit)
          echo "/en/challenges/invalid/edit → $code"
          test "$code" = "404"

      - name: Check invalid solution new 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" https://canaithis.vercel.app/ko/challenges/invalid/solutions/new)
          echo "/ko/challenges/invalid/solutions/new → $code"
          test "$code" = "404"

      - name: Check English invalid solution new 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" https://canaithis.vercel.app/en/challenges/invalid/solutions/new)
          echo "/en/challenges/invalid/solutions/new → $code"
          test "$code" = "404"

      - name: Check invalid solution edit 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" https://canaithis.vercel.app/ko/challenges/invalid/solutions/invalid/edit)
          echo "/ko/challenges/invalid/solutions/invalid/edit → $code"
          test "$code" = "404"

      - name: Check English invalid solution edit 404
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" https://canaithis.vercel.app/en/challenges/invalid/solutions/invalid/edit)
          echo "/en/challenges/invalid/solutions/invalid/edit → $code"
          test "$code" = "404"

      - name: Check robots.txt and Sitemap directive
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" https://canaithis.vercel.app/robots.txt)
          echo "/robots.txt → $code"
          test "$code" = "200"
          robots_body=$(curl -sL https://canaithis.vercel.app/robots.txt)
          echo "$robots_body" | grep -qi "Sitemap:"
          echo "robots.txt → Sitemap directive OK"
          echo "$robots_body" | grep -qE 'Sitemap:\s*https?://[^[:space:]]+/sitemap\.xml'
          echo "robots.txt → sitemap.xml URL OK"

      - name: Check sitemap includes users and challenges
        run: |
          code=$(curl -sL -o /dev/null -w "%{http_code}" https://canaithis.vercel.app/sitemap.xml)
          echo "/sitemap.xml → $code"
          test "$code" = "200"
          sitemap_body=$(curl -sL https://canaithis.vercel.app/sitemap.xml)
          echo "$sitemap_body" | grep -q "/users/"
          echo "sitemap → /users/ OK"
          echo "$sitemap_body" | grep -qE '/challenges/[a-f0-9]{24}'
          echo "sitemap → /challenges/ detail OK"